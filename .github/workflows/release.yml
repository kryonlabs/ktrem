---
name: Build and Release

"on":
  pull_request:
  push:
    branches:
      - master
      - main
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

env:
  KTREM_DIR: ktrem
  KRYON_CHECKOUT_DIR: kryon

jobs:
  linux-build:
    name: Linux build and test
    runs-on: ubuntu-latest

    steps:
      - name: Check out t9
        uses: actions/checkout@v4
        with:
          path: ${{ env.KTREM_DIR }}

      - name: Check out Kryon
        uses: actions/checkout@v4
        with:
          repository: kryonlabs/kryon
          path: ${{ env.KRYON_CHECKOUT_DIR }}
          submodules: recursive

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            ca-certificates \
            cmake \
            git \
            curl \
            desktop-file-utils \
            dpkg-dev \
            fakeroot \
            file \
            libbrotli-dev \
            libdrm-dev \
            libegl1-mesa-dev \
            libgbm-dev \
            libgles2-mesa-dev \
            libgtk-3-dev \
            libssl-dev \
            libsdl2-dev \
            libzstd-dev \
            pkg-config \
            zlib1g-dev

      - name: Build Kryon
        working-directory: ${{ env.KRYON_CHECKOUT_DIR }}
        run: make all

      - name: Test t9
        working-directory: ${{ env.KTREM_DIR }}
        env:
          ENGINE_DIR: ${{ github.workspace }}/${{ env.KRYON_CHECKOUT_DIR }}
        run: make ENGINE_DIR="${ENGINE_DIR}" test

      - name: Build t9
        working-directory: ${{ env.KTREM_DIR }}
        env:
          ENGINE_DIR: ${{ github.workspace }}/${{ env.KRYON_CHECKOUT_DIR }}
        run: make ENGINE_DIR="${ENGINE_DIR}" all

      - name: Package Linux release assets
        working-directory: ${{ env.KTREM_DIR }}
        run: |
          set -eu
          version="${GITHUB_REF_NAME:-${GITHUB_SHA}}"
          version="$(printf '%s' "${version}" | tr '/ :' '---')"
          deb_version="$(printf '%s' "${version}" | sed 's/^v//; s/[^A-Za-z0-9.+:~_-]/-/g')"
          case "${deb_version}" in
            [0-9]*) ;;
            *) deb_version="0.0.0+${deb_version}" ;;
          esac
          package="t9-linux-x86_64-${version}"
          mkdir -p "dist/${package}/bin"
          cp build/linux-x86_64/bin/t9 "dist/${package}/bin/t9"
          ln -s t9 "dist/${package}/bin/ktrem"
          ln -s t9 "dist/${package}/bin/kterm"
          cp build/linux-x86_64/lib/ktrem-host.so "dist/${package}/bin/ktrem-host.so"
          cp README.md "dist/${package}/README.md"
          cp docs/REMAINING_FEATURE_PLAN.md \
            "dist/${package}/REMAINING_FEATURE_PLAN.md"
          if [ -d benchmarks/results ]; then
            mkdir -p "dist/${package}/benchmarks"
            cp benchmarks/README.md "dist/${package}/benchmarks/README.md"
          fi
          tar -C dist -czf "dist/${package}.tar.gz" "${package}"

          deb_root="dist/deb/t9_${deb_version}_amd64"
          mkdir -p "${deb_root}/DEBIAN" \
            "${deb_root}/usr/bin" \
            "${deb_root}/usr/lib/rill/apps" \
            "${deb_root}/usr/share/applications" \
            "${deb_root}/usr/share/doc/t9" \
            "${deb_root}/usr/share/icons/hicolor/scalable/apps"
          cp build/linux-x86_64/bin/t9 "${deb_root}/usr/bin/t9"
          ln -s t9 "${deb_root}/usr/bin/ktrem"
          ln -s t9 "${deb_root}/usr/bin/kterm"
          cp build/linux-x86_64/lib/ktrem-host.so \
            "${deb_root}/usr/lib/rill/apps/ktrem-host.so"
          cp README.md "${deb_root}/usr/share/doc/t9/README.md"
          cp docs/site/favicon.svg \
            "${deb_root}/usr/share/icons/hicolor/scalable/apps/t9.svg"
          cat > "${deb_root}/usr/share/applications/t9.desktop" <<'EOF'
          [Desktop Entry]
          Type=Application
          Name=Terminal
          Comment=Kryon terminal emulator
          Exec=t9
          Icon=t9
          Terminal=false
          Categories=System;TerminalEmulator;
          Keywords=shell;prompt;command;terminal;
          EOF
          cat > "${deb_root}/DEBIAN/control" <<EOF
          Package: t9
          Version: ${deb_version}
          Section: x11
          Priority: optional
          Architecture: amd64
          Maintainer: Kryon Labs <noreply@kryonlabs.com>
          Depends: libc6, libssl3, libsdl2-2.0-0, libgtk-3-0, zlib1g
          Description: Kryon terminal emulator
           t9 is a standalone terminal application using Kryon for windowing,
           rendering, input, and platform integration.
          EOF
          dpkg-deb --build --root-owner-group "${deb_root}" \
            "dist/t9_${deb_version}_amd64.deb"

          appdir="dist/AppDir"
          mkdir -p "${appdir}/usr/bin" \
            "${appdir}/usr/lib/rill/apps" \
            "${appdir}/usr/share/applications" \
            "${appdir}/usr/share/icons/hicolor/scalable/apps"
          cp build/linux-x86_64/bin/t9 "${appdir}/usr/bin/t9"
          ln -s t9 "${appdir}/usr/bin/ktrem"
          ln -s t9 "${appdir}/usr/bin/kterm"
          cp build/linux-x86_64/lib/ktrem-host.so \
            "${appdir}/usr/lib/rill/apps/ktrem-host.so"
          cp docs/site/favicon.svg \
            "${appdir}/usr/share/icons/hicolor/scalable/apps/t9.svg"
          cp "${deb_root}/usr/share/applications/t9.desktop" \
            "${appdir}/usr/share/applications/t9.desktop"
          cp "${appdir}/usr/share/applications/t9.desktop" \
            "${appdir}/t9.desktop"
          ln -s usr/share/icons/hicolor/scalable/apps/t9.svg \
            "${appdir}/t9.svg"
          cat > "${appdir}/AppRun" <<'EOF'
          #!/bin/sh
          HERE="$(dirname "$(readlink -f "$0")")"
          export PATH="${HERE}/usr/bin:${PATH}"
          exec "${HERE}/usr/bin/t9" "$@"
          EOF
          chmod +x "${appdir}/AppRun"
          curl -fL -o /tmp/appimagetool \
            https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage
          chmod +x /tmp/appimagetool
          ARCH=x86_64 /tmp/appimagetool --appimage-extract-and-run \
            "${appdir}" "dist/t9-x86_64-${version}.AppImage"

          sha256sum "dist/${package}.tar.gz" \
            "dist/t9_${deb_version}_amd64.deb" \
            "dist/t9-x86_64-${version}.AppImage" > "dist/SHA256SUMS"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: t9-linux-x86_64
          path: |
            ${{ env.KTREM_DIR }}/dist/*.tar.gz
            ${{ env.KTREM_DIR }}/dist/*.deb
            ${{ env.KTREM_DIR }}/dist/*.AppImage
            ${{ env.KTREM_DIR }}/dist/SHA256SUMS

      - name: Upload release assets
        if: github.event_name == 'release'
        working-directory: ${{ env.KTREM_DIR }}
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: >
          gh release upload "${RELEASE_TAG}"
          dist/*.tar.gz dist/*.deb dist/*.AppImage dist/SHA256SUMS --clobber
